let Observable;_548‍.w("rxjs",[["Observable",["Observable"],function(v){Observable=v}]]);

const output = new Observable(observer => {
  let count = 0;
  const interval = setInterval(() => {
    observer.next(count++);
    observer.complete();
  }, 100);

  // once we stop listening to values clear the interval
  return () => {
    clearInterval(interval);
  };
});

output.subscribe(value => _548‍.g.console.log(value));