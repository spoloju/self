let Observable;_706‍.w("rxjs",[["Observable",["Observable"],function(v){Observable=v}]]);
 
const observable = new Observable(subscriber => {
  subscriber.next(1);
  subscriber.next(2);
  subscriber.next(3);
  setTimeout(() => {
    subscriber.next(4);
    subscriber.complete();
  }, 1000);
});
 
console.log('just before subscribe');
observable.subscribe({
  next(x) { _706‍.g.console.log('got value ' + x); },
  error(err) { _706‍.g.console.error('something wrong occurred: ' + err); },
  complete() { console.log('done'); }
});
console.log('just after subscribe');