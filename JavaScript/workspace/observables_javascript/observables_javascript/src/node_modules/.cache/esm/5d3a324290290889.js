let Observable;_902‍.w("rxjs",[["Observable",["Observable"],function(v){Observable=v}]]);

let observable = new Observable((observer) => {
  setTimeout(() => {
    observer.next("some value")
  }, 1000);
})

observable.subscribe(value => {
  
  _902‍.g.console.log(value)
});