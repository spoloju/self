let Observable;_59a‍.w("rxjs",[["Observable",["Observable"],function(v){Observable=v}]]);

let observable = new Observable((observer) => {
  setTimeout(() => {
    observer.next("some value")
  }, 1000);
})

observable.subscribe(value => {
  
  _59a‍.g.console.log(value)
});